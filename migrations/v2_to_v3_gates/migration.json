{
  "_comment": "Read by scripts/compat_check.py (F28). Phase 8 step 0: a purely additive ontology/shapes evolution (oo:GateUnavailable) — see migrations/v2_to_v3_gates/deploy.py's own docstring for why this is a separate migration directory from migrations/v2_to_v3/ (that one covers actions/authorization reaching different version numbers in the same deploy; this one covers ontology/shapes both reaching v3 for an unrelated reason, on a different day).",
  "from_version": "v2",
  "to_version": "v3",
  "kinds_changed": ["ontology", "shapes"],
  "kind_versions": {
    "ontology": "v3",
    "shapes": "v3"
  },
  "breaking_changes": [
    {
      "kind": "ontology",
      "description": "Adds oo:GateUnavailable (skos:Concept in oo:DecisionStatusScheme) + oo:unavailableGate (unconstrained datatype property). Purely additive — no existing individual, property, or triple removed or redefined. v2/oo-core.ttl is untouched.",
      "removed_properties": []
    },
    {
      "kind": "shapes",
      "description": "decision-shape.ttl's oo:status sh:in enumeration gains oo:GateUnavailable as a 15th allowed value. Every previously-allowed value is unchanged. v2/decision-shape.ttl is untouched.",
      "removed_properties": []
    }
  ],
  "migration_script": "migrations/v2_to_v3_gates/deploy.py",
  "deploy_script": "migrations/v2_to_v3_gates/deploy.py",
  "replay_verified": true,
  "replay_verification_note": "make test-replay replays the full historical corpus (all decisions pinned to v1/v2 ontology+shapes, unaffected since those directories are untouched) plus the F22-F24-derived GATE_UNAVAILABLE/legacy-outage-DENIED_* decisions under their new PASS_FAIL_CLOSED_VERIFIED replay class — see docs/experiment/implementation-notes.md Phase 8 section for the run's pytest summary line."
}
